registry=https://registry.npmjs.org/

# Vite 8 + @vitejs/plugin-react 4 has a stale peer-range; works in practice.
# Without this flag every npm install / npm ci fails on ERESOLVE.
legacy-peer-deps=true
